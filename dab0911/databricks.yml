# This is a Databricks asset bundle definition for dab0911.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab0911
  uuid: 957401e4-0b08-4168-80fa-deaa40ceed19

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use


targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: production
    presets:
      name_prefix: dev_shared_wellstreet
    workspace:
      host: https://dbc-7fcc055b-96dc.cloud.databricks.com
    variables:
      catalog: dab0911
  prod:
    mode: production
    workspace:
      host: https://dbc-7fcc055b-96dc.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/brumiajaffrin@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/brumiajaffrin@gmail.com/PROD3/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dab0911
    permissions:
      - user_name: brumiajaffrin@gmail.com
        level: CAN_MANAGE
