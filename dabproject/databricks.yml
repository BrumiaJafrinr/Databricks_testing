# This is a Databricks asset bundle definition for dabproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabproject
  uuid: 0f487905-be09-4e6c-93d8-b0849754e436


include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: ${workspace.current_user.short_name}
      source_linked_deployment: false
    workspace:
      host: https://dbc-7fcc055b-96dc.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-7fcc055b-96dc.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/brumiajaffrin@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/brumiajaffrin@gmail.com/PROD2/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: brumiajaffrin@gmail.com
        level: CAN_MANAGE
